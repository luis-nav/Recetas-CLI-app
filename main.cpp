#include "app.h"

int main(int argc, char** argv){
    // BaseDeDatos* misResetas = new BaseDeDatos();

    app();
    // cout << inputDouble();

    // cout << misResetas->recetas[0].nombre << endl << "hey" << endl;
    // Receta * miRes = new Receta("Pollito", 2);
    // miRes->anadirIngrediente("Pollo", 1, "kg");
    // miRes->imprimir(miRes->porciones);
    // Ingrediente * pollo = new Ingrediente("Pollo", 1, "kg");

    // (*miRes).ingredientes[0] = *pollo

    
    return 1;
}